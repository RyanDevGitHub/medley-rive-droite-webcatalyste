name: CD - Docker Build & Push (Statique)

on:
  push:
    branches: ["main"] # Déclencher directement sur push vers main

jobs:
  docker-build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read # Lecture du code source
      packages: write #
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔑 Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐳 Build Docker image
        # Utilise le nouveau nom d'image
        run: docker build -t ghcr.io/ryandevgithub/medley-rive-droite-webcatalyste:latest .

      - name: 📤 Push Docker image
        run: docker push ghcr.io/ryandevgithub/medley-rive-droite-webcatalyste:latest

  deploy:
    name: "🚀 Deploy to VPS"
    runs-on: ubuntu-latest
    needs: docker-build-push

    steps:
      - name: 🚀 Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            CONTAINER_NAME="medley-rive-droite-webcatalyste"
            IMAGE_NAME="ghcr.io/ryandevgithub/$CONTAINER_NAME:latest"
             echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin 
               docker stop $CONTAINER_NAME || true
               docker rm $CONTAINER_NAME || true
               docker pull $IMAGE_NAME
               docker run -d \
                 --name $CONTAINER_NAME \
                 --restart unless-stopped \
                 -p 8086:80 \
                 $IMAGE_NAME
               docker logout ghcr.io
               docker ps -f name=$CONTAINER_NAME
